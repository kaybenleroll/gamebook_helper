'use client'

interface Props {
  sessionId: number
}

export default function MapGrid({ sessionId: _sessionId }: Props) {
  return <div className="mt-6 p-4 border border-gray-200 rounded text-gray-500">Map coming soon</div>
}
